selector
    sequence
        cond IsOn(clock,nightstand)
        cond IsClean(nightstand)
    sequence
        cond IsNear(self,nightstand)
        cond IsOn(clock,nightstand)
        cond IsHoldingCleaningTool(self)
        act Wipe(nightstand)
    sequence
        cond IsOn(clock,nightstand)
        cond IsHoldingCleaningTool(self)
        cond IsStanding(self)
        act Walk(nightstand)
    sequence
        cond IsOn(clock,nightstand)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,rag)
        act RightGrab(rag)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(clock,nightstand)
        cond IsStanding(self)
        act Walk(rag)
    sequence
        cond IsLeftHolding(self,clock)
        cond IsRightHandEmpty(self)
        cond IsNear(self,nightstand)
        cond IsStanding(self)
        act LeftPut(clock,nightstand)
    sequence
        cond IsLeftHolding(self,clock)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,nightstand)
        cond IsStanding(self)
        act LeftPut(clock,nightstand)
    sequence
        cond IsLeftHolding(self,clock)
        cond IsNear(self,nightstand)
        cond IsHoldingCleaningTool(self)
        act LeftPut(clock,nightstand)
    sequence
        cond IsLeftHolding(self,clock)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        act Walk(nightstand)
    sequence
        cond IsLeftHolding(self,clock)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,rag)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,clock)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        act Walk(rag)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,clock)
        act LeftGrab(clock)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        act Walk(clock)
