selector
    cond IsIn(chicken,garbagecan)
    sequence
        cond IsRightHolding(self,chicken)
        cond IsOpen(garbagecan)
        cond IsNear(self,garbagecan)
        act RightPutIn(chicken,garbagecan)
    sequence
        cond IsRightHolding(self,chicken)
        cond IsStanding(self)
        cond IsOpen(garbagecan)
        act Walk(garbagecan)
    sequence
        cond IsNear(self,chicken)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(garbagecan)
        act RightGrab(chicken)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(garbagecan)
        act Walk(chicken)
    sequence
        cond IsNear(self,garbagecan)
        cond IsStanding(self)
        cond IsRightHolding(self,remotecontrol)
        cond IsOpen(garbagecan)
        act RightPutIn(remotecontrol,garbagecan)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,remotecontrol)
        cond IsClose(garbagecan)
        cond IsNear(self,garbagecan)
        cond IsStanding(self)
        act Open(garbagecan)
    sequence
        cond IsOpen(coffeemaker)
        cond IsStanding(self)
        cond IsRightHolding(self,remotecontrol)
        cond IsNear(self,coffeemaker)
        cond IsPlugged(coffeemaker)
        cond IsOpen(garbagecan)
        act RightPutIn(remotecontrol,coffeemaker)
    sequence
        cond IsRightHolding(self,pie)
        cond IsNear(self,garbagecan)
        cond IsStanding(self)
        cond IsOpen(garbagecan)
        act RightPutIn(pie,garbagecan)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsClose(garbagecan)
        cond IsRightHolding(self,pie)
        cond IsNear(self,garbagecan)
        cond IsStanding(self)
        act Open(garbagecan)
    sequence
        cond IsNear(self,kitchencabinet)
        cond IsOpen(kitchencabinet)
        cond IsStanding(self)
        cond IsRightHolding(self,milk)
        cond IsOpen(garbagecan)
        act RightPutIn(milk,kitchencabinet)
    sequence
        cond IsPlugged(oven)
        cond IsRightHolding(self,magazine)
        cond IsOpen(oven)
        cond IsOpen(garbagecan)
        cond IsStanding(self)
        cond IsNear(self,oven)
        act RightPutIn(magazine,oven)
    sequence
        cond IsNear(self,garbagecan)
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsOpen(garbagecan)
        act RightPutIn(dishbowl,garbagecan)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,dishbowl)
        cond IsClose(garbagecan)
        cond IsNear(self,garbagecan)
        cond IsStanding(self)
        act Open(garbagecan)
    sequence
        cond IsOpen(fridge)
        cond IsStanding(self)
        cond IsNear(self,fridge)
        cond IsPlugged(fridge)
        cond IsOpen(garbagecan)
        cond IsRightHolding(self,chips)
        act RightPutIn(chips,fridge)
    sequence
        cond IsOpen(coffeemaker)
        cond IsStanding(self)
        cond IsNear(self,coffeemaker)
        cond IsPlugged(coffeemaker)
        cond IsOpen(garbagecan)
        cond IsRightHolding(self,chips)
        act RightPutIn(chips,coffeemaker)
    sequence
        cond IsNear(self,kitchencabinet)
        cond IsOpen(kitchencabinet)
        cond IsStanding(self)
        cond IsRightHolding(self,cellphone)
        cond IsOpen(garbagecan)
        act RightPutIn(cellphone,kitchencabinet)
    sequence
        cond IsRightHolding(self,milk)
        cond IsNear(self,kitchencabinet)
        cond IsOpen(garbagecan)
        cond IsStanding(self)
        act RightPut(milk,kitchencabinet)
    sequence
        cond IsOpen(garbagecan)
        cond IsStanding(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,milk)
        act RightPut(milk,desk)
    sequence
        cond IsNear(self,coffeetable)
        cond IsStanding(self)
        cond IsRightHolding(self,magazine)
        cond IsOpen(garbagecan)
        act RightPut(magazine,coffeetable)
    sequence
        cond IsNear(self,coffeetable)
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsOpen(garbagecan)
        act RightPut(dishbowl,coffeetable)
    sequence
        cond IsNear(self,kitchencabinet)
        cond IsOpen(garbagecan)
        cond IsRightHolding(self,chips)
        cond IsStanding(self)
        act RightPut(chips,kitchencabinet)
    sequence
        cond IsNear(self,coffeetable)
        cond IsStanding(self)
        cond IsRightHolding(self,chips)
        cond IsOpen(garbagecan)
        act RightPut(chips,coffeetable)
    sequence
        cond IsRightHolding(self,cellphone)
        cond IsNear(self,coffeetable)
        cond IsStanding(self)
        cond IsOpen(garbagecan)
        act RightPut(cellphone,coffeetable)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        cond IsClose(garbagecan)
        cond IsNear(self,garbagecan)
        cond IsStanding(self)
        act Open(garbagecan)
    sequence
        cond IsRightHolding(self,chicken)
        cond IsLeftHandEmpty(self)
        cond IsClose(garbagecan)
        cond IsNear(self,garbagecan)
        cond IsStanding(self)
        act Open(garbagecan)
    sequence
        cond IsRightHolding(self,chicken)
        cond IsLeftHandEmpty(self)
        cond IsClose(garbagecan)
        cond IsNear(self,garbagecan)
        act Open(garbagecan)
    sequence
        cond IsRightHolding(self,chicken)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsClose(garbagecan)
        act Walk(garbagecan)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsNear(self,chicken)
        cond IsRightHandEmpty(self)
        cond IsClose(garbagecan)
        cond IsStanding(self)
        act RightGrab(chicken)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsClose(garbagecan)
        act Walk(chicken)
