selector
    sequence
        cond IsOn(plate,kitchentable)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act Wash(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act Wash(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        act Wash(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Wash(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightGrab(plum)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(apple,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightGrab(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsOn(apple,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsClean(peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightGrab(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightGrab(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightGrab(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightGrab(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightGrab(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsOn(apple,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightGrab(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsClean(peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightGrab(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOff(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightGrab(plum)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plum)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightGrab(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightGrab(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightGrab(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,peach)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,peach)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightGrab(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsLeftHolding(self,peach)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsLeftHolding(self,peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightGrab(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,peach)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightGrab(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightGrab(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightGrab(plum)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plum)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightGrab(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act RightGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,apple)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act RightGrab(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Walk(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsLeftHolding(self,apple)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightGrab(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,apple)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act RightGrab(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightGrab(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightGrab(plum)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plum)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightGrab(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(peach,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(peach,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act Walk(kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        act Walk(kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,plate)
        act Walk(kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,peach)
        act Walk(kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,plate)
        act Walk(kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightGrab(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(apple)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightGrab(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(apple)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightGrab(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(apple)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightGrab(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightGrab(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(peach)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightGrab(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightGrab(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightGrab(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightGrab(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightGrab(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(peach,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act Walk(kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightGrab(plate)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act RightGrab(peach)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act RightGrab(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act Walk(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act Walk(kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act Walk(kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightGrab(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightGrab(plum)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plum)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act Walk(kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsNear(self,peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act RightGrab(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act Walk(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,peach)
        act Walk(kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightGrab(plum)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,peach)
        act Walk(plum)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act Walk(kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act RightGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        act Walk(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act SwitchOn(faucet)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act SwitchOn(faucet)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(plum)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightGrab(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,plate)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act RightGrab(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsHoldingCleaningTool(self)
        act RightGrab(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(plum)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsNear(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHandEmpty(self)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsNear(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsNear(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsOn(plum,plate)
        cond IsNear(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsNear(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsNear(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsNear(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsNear(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHandEmpty(self)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(plum)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act Walk(faucet)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(faucet)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act Walk(kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act SwitchOn(faucet)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftGrab(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftGrab(plate)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftGrab(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        act LeftGrab(apple)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftGrab(peach)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftGrab(peach)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftGrab(peach)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftGrab(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftGrab(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsClean(peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsClean(peach)
        act RightGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        act RightGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(peach)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftGrab(plum)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftGrab(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act LeftGrab(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act Walk(apple)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act Walk(kitchentable)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act Walk(peach)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act Walk(kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(apple)
        act LeftGrab(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        act Walk(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(apple)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(apple)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act LeftGrab(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act RightGrab(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(apple)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        act Walk(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(apple)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        act Walk(apple)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(apple)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        act Walk(faucet)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsNear(self,plate)
        act RightGrab(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(apple)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        act Walk(faucet)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        act Walk(kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act LeftGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        act LeftGrab(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftGrab(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(peach)
        act Walk(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        act LeftGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        act Walk(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        act Walk(peach)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        act Walk(faucet)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        act Walk(kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        act LeftGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        act LeftGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act Walk(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act LeftGrab(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        act Walk(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsLeftHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsLeftHolding(self,apple)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,apple)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsLeftHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsLeftHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,peach)
        act RightPut(apple,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsLeftHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,plate)
        act Walk(kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,plate)
        act RightGrab(plum)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,peach)
        act Walk(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsLeftHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsLeftHolding(self,apple)
        act RightGrab(plum)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsLeftHolding(self,apple)
        act Walk(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsLeftHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,peach)
        act RightPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(peach,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsNear(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act RightGrab(plum)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act Walk(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsLeftHolding(self,apple)
        act RightGrab(plum)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsLeftHolding(self,apple)
        act Walk(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act Walk(kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsLeftHolding(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act Walk(kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsLeftHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsLeftHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act RightPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act RightPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(peach,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(peach,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(peach,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act Walk(kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act Walk(kitchentable)
    sequence
        cond IsLeftHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightPut(peach,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act RightPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Wash(apple)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        act RightPut(apple,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act RightPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act RightPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,apple)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act RightPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act RightPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsLeftHolding(self,apple)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsLeftHolding(self,apple)
        act RightGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,apple)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        act Wash(peach)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(peach,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        act RightGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(apple,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(peach,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,peach)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsLeftHolding(self,peach)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHolding(self,peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,peach)
        act RightGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,peach)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act RightPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        act Walk(kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,plate)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(faucet)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act RightPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wash(peach)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wash(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wash(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wash(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsOn(plum,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wash(apple)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,plate)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Walk(kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act RightPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(kitchentable)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act RightGrab(plum)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(plum)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Walk(kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act RightGrab(plum)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(plum)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Walk(plate)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act RightGrab(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act RightGrab(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Walk(apple)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Walk(plate)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act RightGrab(peach)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act RightGrab(plum)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(plum)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act RightGrab(plum)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(plum)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plate,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsHoldingCleaningTool(self)
        act RightPut(plate,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act RightGrab(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(peach)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsNear(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act RightPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act RightPut(apple,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,plate)
        act LeftPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act LeftPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act LeftPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act LeftPut(peach,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        cond IsNear(self,plate)
        act LeftPut(apple,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act LeftPut(apple,kitchentable)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plum,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,kitchentable)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,kitchentable)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act LeftPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsOn(plum,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsLeftHolding(self,plum)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Walk(apple)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(plum)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Walk(apple)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(plum)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Walk(apple)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(plum)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Walk(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Wipe(plum)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Walk(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Walk(plate)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Walk(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Wipe(plum)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Wipe(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Walk(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsNear(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(plum)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightGrab(plate)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,plate)
        cond IsLeftHolding(self,apple)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(plate)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act RightGrab(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plum)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plum)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plum)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsNear(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsNear(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(plum)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Wipe(peach)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsNear(self,plate)
        act RightPut(plum,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsNear(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsNear(self,plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(plum,plate)
        cond IsClean(plum)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(plum)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(apple,plate)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Walk(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsNear(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(plum)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act RightGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(peach,plate)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsLeftHolding(self,plate)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Walk(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,plate)
        act RightPut(apple,plate)
    sequence
        cond IsNear(self,peach)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Walk(peach)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(apple,plate)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        act Walk(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(peach)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsNear(self,peach)
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act RightGrab(peach)
    sequence
        cond IsNear(self,peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act RightGrab(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(peach)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsOn(apple,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(plate)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,plate)
        act RightPut(peach,plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        act RightGrab(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsNear(self,plate)
        cond IsHoldingCleaningTool(self)
        act RightPut(apple,plate)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(plate)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(peach)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(peach)
    sequence
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsNear(self,apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Walk(apple)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Walk(kitchentable)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        act Walk(apple)
    sequence
        cond IsLeftHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,papertowel)
        act Walk(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Walk(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(peach)
    sequence
        cond IsNear(self,kitchentable)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act RightPut(plate,kitchentable)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(kitchentable)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,plate)
        act Walk(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Walk(apple)
    sequence
        cond IsLeftHolding(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        act Walk(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        act Walk(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Wipe(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        act Walk(apple)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHolding(self,plum)
        cond IsRightHolding(self,plate)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act SwitchOff(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(peach,plate)
        cond IsHoldingCleaningTool(self)
        cond IsOn(apple,plate)
        act Walk(peach)
    sequence
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsOn(peach,plate)
        cond IsClean(peach)
        cond IsOn(apple,plate)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act RightGrab(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act RightGrab(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Walk(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(faucet)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plum)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(plum)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Walk(faucet)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Walk(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,apple)
        act Wipe(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsRightHolding(self,papertowel)
        cond IsLeftHolding(self,apple)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Walk(plum)
    sequence
        cond IsNear(self,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act RightPut(plate,kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Walk(kitchentable)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Walk(plum)
    sequence
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(faucet)
    sequence
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsRightHolding(self,plum)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plate)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Wipe(plum)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsHoldingCleaningTool(self)
        act Walk(plum)
    sequence
        cond IsNear(self,peach)
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Wipe(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsOn(plum,plate)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,peach)
        act Walk(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsOn(plate,kitchentable)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsOn(plum,plate)
        cond IsClean(peach)
        cond IsLeftHolding(self,peach)
        cond IsRightHolding(self,papertowel)
        cond IsHoldingCleaningTool(self)
        act Wipe(apple)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsClean(apple)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsClean(apple)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsClean(apple)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        act RightGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        act Walk(peach)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsClean(apple)
        act SwitchOn(faucet)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act RightGrab(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(peach)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Wash(plum)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,plum)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsClean(peach)
        act Wash(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsClean(plum)
        cond IsClean(peach)
        act Walk(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,apple)
        cond IsClean(plum)
        cond IsClean(peach)
        act RightGrab(apple)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(plum)
        cond IsClean(peach)
        act Walk(apple)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        cond IsClean(plum)
        cond IsClean(peach)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,apple)
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsClean(plum)
        cond IsClean(apple)
        cond IsClean(peach)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsClean(peach)
        cond IsRightHolding(self,peach)
        cond IsStanding(self)
        act Walk(plate)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,faucet)
        cond IsRightHolding(self,peach)
        cond IsStanding(self)
        act Wash(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHolding(self,peach)
        cond IsStanding(self)
        act Walk(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        act SwitchOn(faucet)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        act RightGrab(peach)
    sequence
        cond IsSwitchedOn(faucet)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        act Walk(peach)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        act SwitchOn(faucet)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsNear(self,faucet)
        act SwitchOn(faucet)
    sequence
        cond IsRightHolding(self,peach)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsStanding(self)
        act Walk(faucet)
    sequence
        cond IsNear(self,peach)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(faucet)
        cond IsLeftHandEmpty(self)
        act RightGrab(peach)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        cond IsSwitchedOff(faucet)
        cond IsStanding(self)
        act Walk(peach)
