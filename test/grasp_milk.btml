selector
    sequence
        cond IsClean(condimentbottle)
        cond IsIn(juice,fridge)
        cond IsCut(bellpepper)
        cond IsOn(milk,sofa)
    sequence
        cond IsLeftHolding(self,bellpepper)
        cond IsClean(condimentbottle)
        cond IsHoldingKnife(self)
        cond IsIn(juice,fridge)
        cond IsOn(milk,sofa)
        act Cut(bellpepper)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsClean(condimentbottle)
        cond IsHoldingKnife(self)
        cond IsIn(juice,fridge)
        cond IsOn(milk,sofa)
        act LeftGrab(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClean(condimentbottle)
        cond IsHoldingKnife(self)
        cond IsIn(juice,fridge)
        cond IsOn(milk,sofa)
        act Walk(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsClean(condimentbottle)
        cond IsHoldingKnife(self)
        cond IsIn(juice,fridge)
        cond IsLeftHolding(self,rag)
        cond IsOn(milk,sofa)
        act LeftPutIn(rag,sink)
    sequence
        cond IsStanding(self)
        cond IsClean(condimentbottle)
        cond IsHoldingKnife(self)
        cond IsIn(juice,fridge)
        cond IsLeftHolding(self,rag)
        cond IsOn(milk,sofa)
        act Walk(sink)
    sequence
        cond IsNear(self,condimentbottle)
        cond IsStanding(self)
        cond IsLeftHolding(self,rag)
        cond IsHoldingKnife(self)
        cond IsIn(juice,fridge)
        cond IsHoldingCleaningTool(self)
        cond IsOn(milk,sofa)
        act Wipe(condimentbottle)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,rag)
        cond IsHoldingKnife(self)
        cond IsIn(juice,fridge)
        cond IsHoldingCleaningTool(self)
        cond IsOn(milk,sofa)
        act Walk(condimentbottle)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,rag)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsIn(juice,fridge)
        cond IsHoldingCleaningTool(self)
        cond IsOn(milk,sofa)
        act RightGrab(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,rag)
        cond IsRightHandEmpty(self)
        cond IsIn(juice,fridge)
        cond IsHoldingCleaningTool(self)
        cond IsOn(milk,sofa)
        act Walk(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sofa)
        cond IsLeftHolding(self,rag)
        cond IsIn(juice,fridge)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,milk)
        act RightPut(milk,sofa)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,rag)
        cond IsIn(juice,fridge)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,milk)
        act Walk(sofa)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(juice,fridge)
        cond IsNear(self,rag)
        cond IsRightHolding(self,milk)
        act LeftGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(juice,fridge)
        cond IsRightHolding(self,milk)
        act Walk(rag)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        cond IsNear(self,milk)
        cond IsIn(juice,fridge)
        act RightGrab(milk)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(juice,fridge)
        cond IsRightHandEmpty(self)
        act Walk(milk)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(fridge)
        cond IsRightHolding(self,juice)
        cond IsNear(self,fridge)
        cond IsPlugged(fridge)
        act RightPutIn(juice,fridge)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(fridge)
        cond IsRightHolding(self,juice)
        cond IsNear(self,fridge)
        cond IsUnplugged(fridge)
        act PlugIn(fridge)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,juice)
        cond IsSwitchedOff(fridge)
        cond IsNear(self,fridge)
        cond IsUnplugged(fridge)
        cond IsClose(fridge)
        act Open(fridge)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,juice)
        cond IsSwitchedOff(fridge)
        cond IsUnplugged(fridge)
        cond IsClose(fridge)
        act Walk(fridge)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(fridge)
        cond IsRightHandEmpty(self)
        cond IsUnplugged(fridge)
        cond IsClose(fridge)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(fridge)
        cond IsRightHandEmpty(self)
        cond IsUnplugged(fridge)
        cond IsClose(fridge)
        act Walk(juice)
