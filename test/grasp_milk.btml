selector
    sequence
        cond IsSwitchedOn(candle)
        cond IsSwitchedOn(oven)
        cond IsClean(kitchencounter)
        cond IsIn(poundcake,oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsOpen(oven)
        act RightPutIn(poundcake,oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,oven)
        cond IsIn(poundcake,oven)
        cond IsClean(kitchencounter)
        cond IsSwitchedOff(oven)
        act SwitchOn(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        act PlugIn(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsOpen(oven)
        act SwitchOn(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsOpen(oven)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        act Open(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        act SwitchOn(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsOpen(oven)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsOpen(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        act Open(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        act PlugIn(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Open(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        act PlugIn(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsNear(self,poundcake)
        cond IsOpen(oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsStanding(self)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsOpen(oven)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,kitchencounter)
        cond IsPlugged(oven)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsOpen(oven)
        act Wipe(kitchencounter)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act PlugIn(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Open(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsOpen(oven)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsOpen(oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(poundcake)
    sequence
        cond IsOpen(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Walk(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,poundcake)
        cond IsClean(kitchencounter)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsStanding(self)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsNear(self,kitchencounter)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Wipe(kitchencounter)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Walk(kitchencounter)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        act PlugIn(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        act Open(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Open(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(kitchencounter)
        cond IsNear(self,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        cond IsRightHandEmpty(self)
        act PlugIn(oven)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsStanding(self)
        cond IsClean(kitchencounter)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsOpen(oven)
        cond IsRightHandEmpty(self)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act PlugIn(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Open(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsOpen(oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsOpen(oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(oven)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsNear(self,kitchencounter)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Wipe(kitchencounter)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,kitchencounter)
        cond IsPlugged(oven)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(oven)
        cond IsOpen(oven)
        cond IsRightHandEmpty(self)
        act Wipe(kitchencounter)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(kitchencounter)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsOpen(oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(kitchencounter)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act PlugIn(oven)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsNear(self,kitchencounter)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Wipe(kitchencounter)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsStanding(self)
        cond IsNear(self,oven)
        cond IsSwitchedOff(oven)
        cond IsOpen(oven)
        cond IsRightHandEmpty(self)
        act Close(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act PlugIn(oven)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsOpen(oven)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Open(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        act PlugIn(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsNear(self,rag)
        cond IsOpen(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,poundcake)
        cond IsOpen(oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsStanding(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsOpen(oven)
        act SwitchOn(candle)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsNear(self,kitchencounter)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Wipe(kitchencounter)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOn(oven)
        cond IsNear(self,candle)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        act Open(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsStanding(self)
        act Open(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsNear(self,poundcake)
        cond IsOpen(oven)
        cond IsSwitchedOn(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsStanding(self)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsNear(self,candle)
        cond IsOpen(oven)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsOpen(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        act Open(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,kitchencounter)
        cond IsPlugged(oven)
        cond IsStanding(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsOpen(oven)
        act Wipe(kitchencounter)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        act SwitchOn(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(poundcake)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Walk(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Walk(kitchencounter)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsNear(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsNear(self,kitchencounter)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Wipe(kitchencounter)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(kitchencounter)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        act Close(oven)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsNear(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsNear(self,kitchencounter)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Wipe(kitchencounter)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsNear(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsOpen(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(poundcake)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(kitchencounter)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(poundcake)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        act Close(oven)
    sequence
        cond IsClose(oven)
        cond IsNear(self,rag)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        cond IsRightHandEmpty(self)
        act Close(oven)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act SwitchOn(candle)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsOpen(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(poundcake)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(oven)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(candle)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,poundcake)
        cond IsOpen(oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(kitchencounter)
        cond IsNear(self,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        cond IsRightHandEmpty(self)
        act Close(oven)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsNear(self,kitchencounter)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Wipe(kitchencounter)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsNear(self,poundcake)
        cond IsOpen(oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsNear(self,kitchencounter)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        act Wipe(kitchencounter)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act SwitchOn(candle)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act PlugIn(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act PlugIn(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsNear(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsStanding(self)
        act SwitchOn(candle)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsNear(self,kitchencounter)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Wipe(kitchencounter)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Open(oven)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(kitchencounter)
        cond IsNear(self,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        cond IsRightHandEmpty(self)
        act PlugIn(oven)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsNear(self,kitchencounter)
        cond IsPlugged(oven)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(oven)
        cond IsOpen(oven)
        cond IsRightHandEmpty(self)
        act Wipe(kitchencounter)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(oven)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsStanding(self)
        act Walk(kitchencounter)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,kitchencounter)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOn(oven)
        act Wipe(kitchencounter)
    sequence
        cond IsSwitchedOn(candle)
        cond IsPlugged(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsOpen(oven)
        act RightPutIn(poundcake,oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsNear(self,poundcake)
        cond IsOpen(oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsOpen(oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(oven)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsOpen(oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(kitchencounter)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsStanding(self)
        act Open(oven)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsNear(self,kitchencounter)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        cond IsRightHandEmpty(self)
        act Wipe(kitchencounter)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Open(oven)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        cond IsRightHandEmpty(self)
        act PlugIn(oven)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsOpen(oven)
        cond IsSwitchedOff(candle)
        cond IsPlugged(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act RightPutIn(poundcake,oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act PlugIn(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsOpen(oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(kitchencounter)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(oven)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsOpen(oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(oven)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Open(oven)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsOpen(oven)
        cond IsSwitchedOff(candle)
        cond IsPlugged(oven)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(oven)
        cond IsSwitchedOff(candle)
        cond IsPlugged(oven)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsClose(oven)
        cond IsNear(self,rag)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsOpen(oven)
        cond IsSwitchedOff(candle)
        cond IsPlugged(oven)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        act Close(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(oven)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsNear(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsNear(self,candle)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsNear(self,rag)
        cond IsOpen(oven)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,poundcake)
        cond IsOpen(oven)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsNear(self,candle)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(poundcake)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsOpen(oven)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,oven)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        cond IsRightHandEmpty(self)
        act Close(oven)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOn(oven)
        cond IsNear(self,candle)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act SwitchOn(candle)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        act Close(oven)
    sequence
        cond IsClose(oven)
        cond IsNear(self,rag)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(oven)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(oven)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(candle)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsOpen(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOn(oven)
        cond IsNear(self,candle)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        cond IsRightHandEmpty(self)
        act SwitchOn(candle)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,oven)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        cond IsRightHandEmpty(self)
        act Close(oven)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,poundcake)
        cond IsOpen(oven)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(oven)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(poundcake)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsOpen(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(candle)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsNear(self,candle)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsNear(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsNear(self,candle)
        cond IsStanding(self)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        act PlugIn(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsNear(self,rag)
        cond IsOpen(oven)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsPlugged(oven)
        cond IsNear(self,poundcake)
        cond IsOpen(oven)
        cond IsSwitchedOn(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsStanding(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsNear(self,candle)
        cond IsOpen(oven)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOn(oven)
        cond IsStanding(self)
        act Walk(kitchencounter)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,kitchencounter)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        act Wipe(kitchencounter)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOn(oven)
        cond IsStanding(self)
        act Walk(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsOpen(oven)
        cond IsSwitchedOn(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(poundcake)
    sequence
        cond IsOpen(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsStanding(self)
        act Walk(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsNear(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsNear(self,candle)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsNear(self,kitchencounter)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOn(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Wipe(kitchencounter)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act PlugIn(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsOpen(oven)
        cond IsPlugged(oven)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsOpen(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Walk(candle)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(kitchencounter)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Open(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(oven)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Open(oven)
    sequence
        cond IsOpen(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsOpen(oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(poundcake)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsSwitchedOn(candle)
        cond IsIn(poundcake,oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act PlugIn(oven)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsStanding(self)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,kitchencounter)
        cond IsPlugged(oven)
        cond IsLeftHandEmpty(self)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Wipe(kitchencounter)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsClean(kitchencounter)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Walk(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Walk(kitchencounter)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,kitchencounter)
        cond IsLeftHandEmpty(self)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Wipe(kitchencounter)
    sequence
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsClean(kitchencounter)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act PlugIn(oven)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsNear(self,kitchencounter)
        cond IsPlugged(oven)
        cond IsLeftHandEmpty(self)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Wipe(kitchencounter)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act PlugIn(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsNear(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsStanding(self)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(kitchencounter)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsClean(kitchencounter)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Walk(kitchencounter)
    sequence
        cond IsSwitchedOn(candle)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Walk(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsNear(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsSwitchedOn(candle)
        cond IsNear(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act PlugIn(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsOpen(oven)
        cond IsPlugged(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act RightPutIn(poundcake,oven)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act PlugIn(oven)
    sequence
        cond IsNear(self,rag)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsHoldingCleaningTool(self)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsOpen(oven)
        cond IsPlugged(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act RightPutIn(poundcake,oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Open(oven)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act PlugIn(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsIn(poundcake,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        act Close(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsStanding(self)
        act SwitchOn(candle)
    sequence
        cond IsOpen(oven)
        cond IsSwitchedOff(candle)
        cond IsPlugged(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act RightPutIn(poundcake,oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsOpen(oven)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(poundcake)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Walk(candle)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Open(oven)
    sequence
        cond IsOpen(oven)
        cond IsSwitchedOff(candle)
        cond IsPlugged(oven)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,poundcake)
        cond IsOpen(oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        act SwitchOn(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsClose(oven)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(poundcake)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsStanding(self)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsOpen(oven)
        act Close(oven)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsOpen(oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(poundcake)
    sequence
        cond IsOpen(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Walk(candle)
    sequence
        cond IsSwitchedOn(candle)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        cond IsRightHandEmpty(self)
        act Close(oven)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act SwitchOn(candle)
    sequence
        cond IsOpen(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act PlugIn(oven)
    sequence
        cond IsOpen(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsPlugged(oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsStanding(self)
        act Walk(oven)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(oven)
        cond IsNear(self,candle)
        cond IsUnplugged(oven)
        cond IsOpen(oven)
        cond IsRightHandEmpty(self)
        act SwitchOn(candle)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,oven)
        cond IsRightHolding(self,poundcake)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        act Open(oven)
    sequence
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,poundcake)
        cond IsOpen(oven)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act RightGrab(poundcake)
    sequence
        cond IsClose(oven)
        cond IsSwitchedOff(candle)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(oven)
        cond IsUnplugged(oven)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(candle)
