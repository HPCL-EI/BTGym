selector
    cond IsSwitchedOn(tv)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsPlugged(tv)
        cond IsSwitchedOff(tv)
        cond IsNear(self,tv)
        act SwitchOn(tv)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(tv)
        cond IsUnplugged(tv)
        cond IsNear(self,tv)
        act PlugIn(tv)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsPlugged(tv)
        cond IsSwitchedOff(tv)
        cond IsStanding(self)
        act Walk(tv)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(tv)
        cond IsUnplugged(tv)
        cond IsStanding(self)
        act Walk(tv)
