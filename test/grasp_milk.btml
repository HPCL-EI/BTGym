selector
    sequence
        cond IsSwitchedOn(remotecontrol)
        cond IsPlugged(tv)
        cond IsOn(pillow,sofa)
        cond IsSwitchedOn(tv)
    sequence
        cond IsSwitchedOn(remotecontrol)
        cond IsOn(pillow,sofa)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,tv)
        cond IsSwitchedOff(tv)
        cond IsPlugged(tv)
        act SwitchOn(tv)
    sequence
        cond IsSwitchedOn(remotecontrol)
        cond IsOn(pillow,sofa)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(tv)
        cond IsPlugged(tv)
        act Walk(tv)
    sequence
        cond IsNear(self,remotecontrol)
        cond IsSwitchedOff(remotecontrol)
        cond IsPlugged(tv)
        cond IsOn(pillow,sofa)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(tv)
        cond IsStanding(self)
        act SwitchOn(remotecontrol)
    sequence
        cond IsSwitchedOff(remotecontrol)
        cond IsOn(pillow,sofa)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(tv)
        cond IsPlugged(tv)
        act Walk(remotecontrol)
    sequence
        cond IsSwitchedOff(remotecontrol)
        cond IsRightHolding(self,pillow)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(tv)
        cond IsNear(self,sofa)
        cond IsPlugged(tv)
        act RightPut(pillow,sofa)
    sequence
        cond IsSwitchedOff(remotecontrol)
        cond IsRightHolding(self,pillow)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(tv)
        cond IsPlugged(tv)
        act Walk(sofa)
    sequence
        cond IsSwitchedOff(remotecontrol)
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(tv)
        cond IsPlugged(tv)
        cond IsNear(self,pillow)
        act RightGrab(pillow)
    sequence
        cond IsSwitchedOff(remotecontrol)
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(tv)
        cond IsPlugged(tv)
        act Walk(pillow)
    sequence
        cond IsUnplugged(tv)
        cond IsSwitchedOff(remotecontrol)
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        cond IsNear(self,tv)
        cond IsSwitchedOff(tv)
        cond IsStanding(self)
        act PlugIn(tv)
    sequence
        cond IsUnplugged(tv)
        cond IsSwitchedOff(remotecontrol)
        cond IsRightHolding(self,pillow)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,tv)
        cond IsSwitchedOff(tv)
        cond IsStanding(self)
        act PlugIn(tv)
    sequence
        cond IsUnplugged(tv)
        cond IsSwitchedOff(remotecontrol)
        cond IsOn(pillow,sofa)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,tv)
        cond IsSwitchedOff(tv)
        cond IsStanding(self)
        act PlugIn(tv)
    sequence
        cond IsSwitchedOn(remotecontrol)
        cond IsRightHolding(self,pillow)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(tv)
        cond IsNear(self,sofa)
        cond IsPlugged(tv)
        act RightPut(pillow,sofa)
    sequence
        cond IsSwitchedOn(remotecontrol)
        cond IsRightHolding(self,pillow)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(tv)
        cond IsPlugged(tv)
        act Walk(sofa)
    sequence
        cond IsSwitchedOn(remotecontrol)
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(tv)
        cond IsPlugged(tv)
        cond IsNear(self,pillow)
        act RightGrab(pillow)
    sequence
        cond IsSwitchedOn(remotecontrol)
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsSwitchedOff(tv)
        cond IsPlugged(tv)
        act Walk(pillow)
    sequence
        cond IsUnplugged(tv)
        cond IsSwitchedOn(remotecontrol)
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        cond IsNear(self,tv)
        cond IsSwitchedOff(tv)
        cond IsStanding(self)
        act PlugIn(tv)
    sequence
        cond IsUnplugged(tv)
        cond IsSwitchedOn(remotecontrol)
        cond IsRightHolding(self,pillow)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,tv)
        cond IsSwitchedOff(tv)
        cond IsStanding(self)
        act PlugIn(tv)
    sequence
        cond IsUnplugged(tv)
        cond IsSwitchedOn(remotecontrol)
        cond IsOn(pillow,sofa)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,tv)
        cond IsSwitchedOff(tv)
        cond IsStanding(self)
        act PlugIn(tv)
    sequence
        cond IsUnplugged(tv)
        cond IsSwitchedOn(remotecontrol)
        cond IsOn(pillow,sofa)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,tv)
        cond IsSwitchedOff(tv)
        act PlugIn(tv)
    sequence
        cond IsUnplugged(tv)
        cond IsSwitchedOn(remotecontrol)
        cond IsOn(pillow,sofa)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(tv)
        cond IsStanding(self)
        act Walk(tv)
    sequence
        cond IsNear(self,remotecontrol)
        cond IsUnplugged(tv)
        cond IsSwitchedOff(remotecontrol)
        cond IsOn(pillow,sofa)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(tv)
        cond IsStanding(self)
        act SwitchOn(remotecontrol)
    sequence
        cond IsUnplugged(tv)
        cond IsSwitchedOff(remotecontrol)
        cond IsOn(pillow,sofa)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(tv)
        cond IsStanding(self)
        act Walk(remotecontrol)
    sequence
        cond IsUnplugged(tv)
        cond IsSwitchedOff(remotecontrol)
        cond IsRightHolding(self,pillow)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(tv)
        cond IsNear(self,sofa)
        cond IsStanding(self)
        act RightPut(pillow,sofa)
    sequence
        cond IsUnplugged(tv)
        cond IsSwitchedOff(remotecontrol)
        cond IsRightHolding(self,pillow)
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(tv)
        cond IsStanding(self)
        act Walk(sofa)
    sequence
        cond IsUnplugged(tv)
        cond IsSwitchedOff(remotecontrol)
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        cond IsSwitchedOff(tv)
        cond IsStanding(self)
        cond IsNear(self,pillow)
        act RightGrab(pillow)
    sequence
        cond IsUnplugged(tv)
        cond IsSwitchedOff(remotecontrol)
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        cond IsSwitchedOff(tv)
        cond IsStanding(self)
        act Walk(pillow)
