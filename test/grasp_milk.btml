selector
    sequence
        cond IsIn(dishbowl,sink)
        cond IsIn(plate,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsIn(washingsponge,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsIn(plate,sink)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsIn(plate,sink)
        act Walk(sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act Walk(sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(plate,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(plate,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act Walk(sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsNear(self,plate)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act LeftGrab(plate)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act Walk(sink)
    sequence
        cond IsNear(self,dishwashingliquid)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        cond IsLeftHandEmpty(self)
        act LeftGrab(dishwashingliquid)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        cond IsLeftHandEmpty(self)
        act Walk(dishwashingliquid)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(plate,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHandEmpty(self)
        cond IsNear(self,washingsponge)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsIn(plate,sink)
        act RightGrab(washingsponge)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsIn(plate,sink)
        act Walk(washingsponge)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsStanding(self)
        cond IsIn(plate,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(dishwashingliquid,sink)
        act Walk(sink)
    sequence
        cond IsNear(self,dishbowl)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsIn(plate,sink)
        act RightGrab(dishbowl)
    sequence
        cond IsStanding(self)
        cond IsIn(plate,sink)
        cond IsRightHandEmpty(self)
        cond IsIn(dishwashingliquid,sink)
        act Walk(dishbowl)
    sequence
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act LeftPutIn(plate,sink)
    sequence
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(plate,sink)
    sequence
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(plate,sink)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(dishwashingliquid,sink)
        cond IsLeftHolding(self,plate)
        act Walk(sink)
    sequence
        cond IsNear(self,dishbowl)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act RightGrab(dishbowl)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act Walk(dishbowl)
    sequence
        cond IsNear(self,plate)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsLeftHandEmpty(self)
        act LeftGrab(plate)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(dishwashingliquid,sink)
        act Walk(plate)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,dishwashingliquid)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsNear(self,plate)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act LeftGrab(plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        cond IsIn(dishwashingliquid,sink)
        act Walk(plate)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHolding(self,dishwashingliquid)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsStanding(self)
        cond IsIn(plate,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHolding(self,dishwashingliquid)
        act Walk(sink)
    sequence
        cond IsNear(self,dishbowl)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsIn(plate,sink)
        act RightGrab(dishbowl)
    sequence
        cond IsStanding(self)
        cond IsIn(plate,sink)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,dishwashingliquid)
        act Walk(dishbowl)
    sequence
        cond IsNear(self,dishwashingliquid)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsIn(plate,sink)
        cond IsLeftHandEmpty(self)
        act LeftGrab(dishwashingliquid)
    sequence
        cond IsStanding(self)
        cond IsIn(plate,sink)
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        act Walk(dishwashingliquid)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHandEmpty(self)
        act LeftPutIn(plate,sink)
    sequence
        cond IsNear(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        cond IsLeftHandEmpty(self)
        act LeftGrab(dishwashingliquid)
    sequence
        cond IsStanding(self)
        cond IsIn(plate,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act Walk(dishwashingliquid)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(plate,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act LeftPutIn(plate,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act LeftPutIn(plate,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsNear(self,plate)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsLeftHandEmpty(self)
        act LeftGrab(plate)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsLeftHandEmpty(self)
        act Walk(plate)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,washingsponge)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        act RightGrab(washingsponge)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        act Walk(washingsponge)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsIn(plate,sink)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsNear(self,dishwashingliquid)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsIn(plate,sink)
        cond IsLeftHandEmpty(self)
        act LeftGrab(dishwashingliquid)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsIn(plate,sink)
        cond IsLeftHandEmpty(self)
        act Walk(dishwashingliquid)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(plate,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(plate,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act Walk(sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act LeftPutIn(plate,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(plate,sink)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act Walk(sink)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsNear(self,dishbowl)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        act RightGrab(dishbowl)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        act Walk(dishbowl)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,plate)
        cond IsLeftHolding(self,dishwashingliquid)
        act Walk(sink)
    sequence
        cond IsRightHandEmpty(self)
        cond IsNear(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        act RightGrab(washingsponge)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,dishwashingliquid)
        act Walk(washingsponge)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsIn(washingsponge,sink)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act LeftPutIn(plate,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsIn(washingsponge,sink)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsIn(washingsponge,sink)
        cond IsNear(self,plate)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsLeftHandEmpty(self)
        act LeftGrab(plate)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsIn(washingsponge,sink)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsLeftHandEmpty(self)
        act Walk(plate)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsStanding(self)
        cond IsIn(dishbowl,sink)
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsNear(self,dishwashingliquid)
        cond IsIn(washingsponge,sink)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        act LeftGrab(dishwashingliquid)
    sequence
        cond IsStanding(self)
        cond IsIn(dishbowl,sink)
        cond IsLeftHandEmpty(self)
        cond IsIn(washingsponge,sink)
        act Walk(dishwashingliquid)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHandEmpty(self)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        cond IsNear(self,sink)
        act RightPutIn(plate,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        cond IsNear(self,sink)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsNear(self,dishwashingliquid)
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        act LeftGrab(dishwashingliquid)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        act Walk(dishwashingliquid)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        act Walk(sink)
    sequence
        cond IsNear(self,dishwashingliquid)
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act LeftGrab(dishwashingliquid)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act Walk(dishwashingliquid)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        act Walk(sink)
    sequence
        cond IsNear(self,dishwashingliquid)
        cond IsIn(washingsponge,sink)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act LeftGrab(dishwashingliquid)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act Walk(dishwashingliquid)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsIn(plate,sink)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(plate,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        act Walk(sink)
    sequence
        cond IsLeftHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsLeftHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(plate,sink)
    sequence
        cond IsLeftHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(plate,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsIn(plate,sink)
        act Walk(sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsIn(plate,sink)
        act Walk(sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(plate,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        act Walk(sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(plate,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(plate,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHolding(self,washingsponge)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsIn(washingsponge,sink)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,washingsponge)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,dishbowl)
        cond IsIn(washingsponge,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsIn(washingsponge,sink)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(plate,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsStanding(self)
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        act Walk(sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,dishbowl)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsIn(washingsponge,sink)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsIn(washingsponge,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHandEmpty(self)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsStanding(self)
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,washingsponge)
        cond IsIn(dishwashingliquid,sink)
        act Walk(sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsIn(washingsponge,sink)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsLeftHolding(self,dishbowl)
        cond IsIn(washingsponge,sink)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsIn(washingsponge,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsIn(washingsponge,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsIn(washingsponge,sink)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsIn(washingsponge,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsNear(self,plate)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        act LeftGrab(plate)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act Walk(sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsNear(self,plate)
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act LeftGrab(plate)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act Walk(plate)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act Walk(sink)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsNear(self,plate)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act LeftGrab(plate)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act Walk(plate)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,washingsponge)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,washingsponge)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsIn(washingsponge,sink)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act Walk(sink)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(plate,sink)
    sequence
        cond IsLeftHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act RightPutIn(plate,sink)
    sequence
        cond IsRightHolding(self,dishwashingliquid)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act RightPutIn(plate,sink)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        act Walk(sink)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        act Walk(sink)
    sequence
        cond IsLeftHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsLeftHolding(self,washingsponge)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsRightHolding(self,plate)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(plate,sink)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act Walk(sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act Walk(sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,plate)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act Walk(sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act Walk(sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,washingsponge)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        cond IsNear(self,sink)
        act RightPutIn(plate,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(plate,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,plate)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(plate,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHandEmpty(self)
        act RightPutIn(plate,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act RightPutIn(plate,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHandEmpty(self)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        act RightPutIn(plate,sink)
    sequence
        cond IsStanding(self)
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsNear(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        act LeftGrab(dishwashingliquid)
    sequence
        cond IsStanding(self)
        cond IsIn(dishbowl,sink)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        act Walk(dishwashingliquid)
    sequence
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(plate,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsIn(plate,sink)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsNear(self,dishwashingliquid)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsIn(plate,sink)
        cond IsLeftHandEmpty(self)
        act LeftGrab(dishwashingliquid)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsIn(plate,sink)
        cond IsLeftHandEmpty(self)
        act Walk(dishwashingliquid)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act LeftPutIn(plate,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act RightPutIn(plate,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,plate)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsNear(self,plate)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        act LeftGrab(plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsIn(dishbowl,sink)
        cond IsLeftHandEmpty(self)
        act Walk(plate)
    sequence
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act RightPutIn(plate,sink)
    sequence
        cond IsLeftHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,plate)
        act RightPutIn(plate,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsLeftHolding(self,plate)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,washingsponge)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHolding(self,plate)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,plate)
        act RightPutIn(plate,sink)
    sequence
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        cond IsLeftHandEmpty(self)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsLeftHolding(self,washingsponge)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        cond IsLeftHandEmpty(self)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        cond IsLeftHandEmpty(self)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        act RightPutIn(plate,sink)
    sequence
        cond IsRightHolding(self,dishwashingliquid)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsLeftHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        act RightPutIn(plate,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsLeftHolding(self,dishwashingliquid)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,washingsponge)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHolding(self,dishwashingliquid)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsLeftHandEmpty(self)
        act RightPutIn(plate,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsLeftHandEmpty(self)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsLeftHolding(self,washingsponge)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsLeftHandEmpty(self)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act RightPutIn(plate,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsLeftHolding(self,plate)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHolding(self,plate)
        act Walk(sink)
    sequence
        cond IsNear(self,plate)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act LeftGrab(plate)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act Walk(plate)
    sequence
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(plate,sink)
    sequence
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        act RightPutIn(plate,sink)
    sequence
        cond IsRightHolding(self,dishwashingliquid)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act RightPutIn(plate,sink)
    sequence
        cond IsRightHolding(self,dishwashingliquid)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act RightPutIn(plate,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsLeftHolding(self,plate)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,plate)
        act RightPutIn(plate,sink)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsIn(plate,sink)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsIn(plate,sink)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(plate,sink)
    sequence
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(plate,sink)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,plate)
        act Walk(sink)
    sequence
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsStanding(self)
        cond IsIn(plate,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsRightHolding(self,dishbowl)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        act RightPutIn(plate,sink)
    sequence
        cond IsStanding(self)
        cond IsIn(dishbowl,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,plate)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsStanding(self)
        cond IsIn(plate,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsIn(dishbowl,sink)
        act Walk(sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsIn(plate,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act RightPutIn(plate,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(plate,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHolding(self,plate)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(plate,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsRightHolding(self,dishbowl)
        act RightPutIn(plate,sink)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,plate)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        act RightPutIn(plate,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsIn(plate,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsIn(plate,sink)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,plate)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        act RightPutIn(plate,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsIn(plate,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsIn(washingsponge,sink)
        cond IsRightHolding(self,plate)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        act RightPutIn(plate,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsIn(washingsponge,sink)
        cond IsRightHolding(self,dishwashingliquid)
        cond IsNear(self,sink)
        cond IsIn(plate,sink)
        act RightPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,washingsponge)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsIn(plate,sink)
        act LeftPutIn(washingsponge,sink)
    sequence
        cond IsLeftHolding(self,dishbowl)
        cond IsIn(washingsponge,sink)
        cond IsNear(self,sink)
        cond IsIn(dishwashingliquid,sink)
        cond IsIn(plate,sink)
        act LeftPutIn(dishbowl,sink)
    sequence
        cond IsNear(self,dishbowl)
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsIn(plate,sink)
        cond IsLeftHandEmpty(self)
        act LeftGrab(dishbowl)
    sequence
        cond IsRightHolding(self,washingsponge)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsIn(plate,sink)
        cond IsLeftHandEmpty(self)
        act Walk(dishbowl)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsNear(self,dishwashingliquid)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsIn(plate,sink)
        act RightGrab(dishwashingliquid)
    sequence
        cond IsStanding(self)
        cond IsIn(plate,sink)
        cond IsIn(dishbowl,sink)
        cond IsRightHandEmpty(self)
        act Walk(dishwashingliquid)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(plate,sink)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsNear(self,plate)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        act RightGrab(plate)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(dishbowl,sink)
        cond IsIn(dishwashingliquid,sink)
        act Walk(plate)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsIn(dishwashingliquid,sink)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsNear(self,dishwashingliquid)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(dishwashingliquid)
    sequence
        cond IsStanding(self)
        cond IsIn(dishbowl,sink)
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        act Walk(dishwashingliquid)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act RightPutIn(dishbowl,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsNear(self,dishwashingliquid)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        act RightGrab(dishwashingliquid)
    sequence
        cond IsStanding(self)
        cond IsIn(dishbowl,sink)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,plate)
        act Walk(dishwashingliquid)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsNear(self,plate)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(plate)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsNear(self,plate)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        act RightGrab(plate)
    sequence
        cond IsStanding(self)
        cond IsIn(dishbowl,sink)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,dishwashingliquid)
        act Walk(plate)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsNear(self,dishwashingliquid)
        cond IsIn(washingsponge,sink)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        act RightGrab(dishwashingliquid)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsNear(self,dishbowl)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsLeftHandEmpty(self)
        act LeftGrab(dishbowl)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsIn(washingsponge,sink)
        act Walk(dishbowl)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(washingsponge,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsNear(self,washingsponge)
        cond IsStanding(self)
        cond IsIn(dishwashingliquid,sink)
        cond IsLeftHandEmpty(self)
        act LeftGrab(washingsponge)
    sequence
        cond IsStanding(self)
        cond IsIn(dishbowl,sink)
        cond IsLeftHandEmpty(self)
        cond IsIn(dishwashingliquid,sink)
        act Walk(washingsponge)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        act LeftPutIn(dishwashingliquid,sink)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        cond IsIn(washingsponge,sink)
        act Walk(sink)
    sequence
        cond IsIn(washingsponge,sink)
        cond IsNear(self,dishbowl)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(dishbowl)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        cond IsIn(washingsponge,sink)
        act Walk(dishbowl)
    sequence
        cond IsStanding(self)
        cond IsNear(self,sink)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,washingsponge)
        act RightPutIn(washingsponge,sink)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,washingsponge)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHandEmpty(self)
        act Walk(sink)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,washingsponge)
        cond IsIn(dishbowl,sink)
        cond IsLeftHandEmpty(self)
        act Walk(sink)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,washingsponge)
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,plate)
        act Walk(sink)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,washingsponge)
        cond IsIn(dishbowl,sink)
        cond IsLeftHolding(self,dishwashingliquid)
        act Walk(sink)
    sequence
        cond IsIn(dishbowl,sink)
        cond IsRightHolding(self,washingsponge)
        cond IsLeftHolding(self,dishwashingliquid)
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        act Walk(sink)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,dishbowl)
        cond IsLeftHolding(self,plate)
        act Walk(sink)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,plate)
        cond IsNear(self,dishbowl)
        act RightGrab(dishbowl)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,plate)
        cond IsRightHandEmpty(self)
        act Walk(dishbowl)
    sequence
        cond IsStanding(self)
        cond IsNear(self,plate)
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        act LeftGrab(plate)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        act Walk(plate)
