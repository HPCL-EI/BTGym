selector
    sequence
        cond IsIn(milk,fridge)
        cond IsClose(fridge)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsIn(milk,fridge)
        cond IsOpen(fridge)
        cond IsNear(self,fridge)
        act Close(fridge)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsOpen(fridge)
        cond IsRightHolding(self,milk)
        cond IsNear(self,fridge)
        cond IsPlugged(fridge)
        act RightPutIn(milk,fridge)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsOpen(fridge)
        cond IsRightHolding(self,milk)
        cond IsUnplugged(fridge)
        cond IsNear(self,fridge)
        act PlugIn(fridge)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,milk)
        cond IsUnplugged(fridge)
        cond IsClose(fridge)
        cond IsNear(self,fridge)
        act Open(fridge)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,milk)
        cond IsClose(fridge)
        cond IsNear(self,fridge)
        cond IsPlugged(fridge)
        act Open(fridge)
    sequence
        cond IsOpen(fridge)
        cond IsRightHolding(self,milk)
        cond IsLeftHolding(self,milk)
        cond IsNear(self,fridge)
        cond IsPlugged(fridge)
        act LeftPutIn(milk,fridge)
    sequence
        cond IsOpen(fridge)
        cond IsLeftHolding(self,wine)
        cond IsRightHolding(self,milk)
        cond IsNear(self,fridge)
        cond IsPlugged(fridge)
        act LeftPutIn(wine,fridge)
    sequence
        cond IsOpen(fridge)
        cond IsNear(self,fridge)
        cond IsPlugged(fridge)
        cond IsLeftHolding(self,milk)
        act LeftPutIn(milk,fridge)
    sequence
        cond IsOpen(fridge)
        cond IsLeftHolding(self,wine)
        cond IsNear(self,fridge)
        cond IsIn(milk,fridge)
        cond IsPlugged(fridge)
        act LeftPutIn(wine,fridge)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(fridge)
        cond IsRightHolding(self,milk)
        cond IsClose(fridge)
        cond IsNear(self,fridge)
        cond IsPlugged(fridge)
        act PlugOut(fridge)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsOpen(fridge)
        cond IsRightHolding(self,milk)
        cond IsUnplugged(fridge)
        cond IsStanding(self)
        act Walk(fridge)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsOpen(fridge)
        cond IsRightHolding(self,milk)
        cond IsStanding(self)
        cond IsPlugged(fridge)
        act Walk(fridge)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsIn(milk,fridge)
        cond IsOpen(fridge)
        cond IsStanding(self)
        act Walk(fridge)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,milk)
        cond IsStanding(self)
        cond IsClose(fridge)
        cond IsPlugged(fridge)
        act Walk(fridge)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,milk)
        cond IsUnplugged(fridge)
        cond IsStanding(self)
        cond IsClose(fridge)
        act Walk(fridge)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsOpen(fridge)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsPlugged(fridge)
        cond IsNear(self,milk)
        act RightGrab(milk)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsOpen(fridge)
        cond IsUnplugged(fridge)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsNear(self,milk)
        act RightGrab(milk)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsUnplugged(fridge)
        cond IsStanding(self)
        cond IsClose(fridge)
        cond IsRightHandEmpty(self)
        cond IsNear(self,milk)
        act RightGrab(milk)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsClose(fridge)
        cond IsRightHandEmpty(self)
        cond IsPlugged(fridge)
        cond IsNear(self,milk)
        act RightGrab(milk)
    sequence
        cond IsOpen(fridge)
        cond IsLeftHolding(self,wine)
        cond IsStanding(self)
        cond IsIn(milk,fridge)
        cond IsPlugged(fridge)
        act Walk(fridge)
    sequence
        cond IsStanding(self)
        cond IsOpen(fridge)
        cond IsPlugged(fridge)
        cond IsLeftHolding(self,milk)
        act Walk(fridge)
    sequence
        cond IsOpen(fridge)
        cond IsLeftHolding(self,wine)
        cond IsRightHolding(self,milk)
        cond IsStanding(self)
        cond IsPlugged(fridge)
        act Walk(fridge)
    sequence
        cond IsOpen(fridge)
        cond IsRightHolding(self,milk)
        cond IsLeftHolding(self,milk)
        cond IsStanding(self)
        cond IsPlugged(fridge)
        act Walk(fridge)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsSwitchedOff(fridge)
        cond IsRightHolding(self,milk)
        cond IsStanding(self)
        cond IsClose(fridge)
        cond IsPlugged(fridge)
        act Walk(fridge)
    sequence
        cond IsOpen(fridge)
        cond IsLeftHolding(self,wine)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsPlugged(fridge)
        cond IsNear(self,milk)
        act RightGrab(milk)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsOpen(fridge)
        cond IsStanding(self)
        cond IsPlugged(fridge)
        cond IsNear(self,milk)
        act LeftGrab(milk)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsOpen(fridge)
        cond IsUnplugged(fridge)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        act Walk(milk)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsOpen(fridge)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsPlugged(fridge)
        act Walk(milk)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsClose(fridge)
        cond IsStanding(self)
        cond IsNear(self,fridge)
        cond IsRightHandEmpty(self)
        cond IsPlugged(fridge)
        act Open(fridge)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsUnplugged(fridge)
        cond IsClose(fridge)
        cond IsStanding(self)
        cond IsNear(self,fridge)
        cond IsRightHandEmpty(self)
        act PlugIn(fridge)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsUnplugged(fridge)
        cond IsClose(fridge)
        cond IsStanding(self)
        cond IsNear(self,fridge)
        cond IsRightHandEmpty(self)
        act Open(fridge)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsClose(fridge)
        cond IsRightHandEmpty(self)
        cond IsPlugged(fridge)
        act Walk(milk)
    sequence
        cond IsLeftHandEmpty(self)
        cond IsUnplugged(fridge)
        cond IsStanding(self)
        cond IsClose(fridge)
        cond IsRightHandEmpty(self)
        act Walk(milk)
