selector
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightGrab(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act Close(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,mincedmeat)
        act LeftGrab(mincedmeat)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(mincedmeat)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,carrot)
        act LeftGrab(carrot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(carrot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftGrab(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Cut(carrot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act Close(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Cut(carrot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act Close(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,mincedmeat)
        act LeftGrab(mincedmeat)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(mincedmeat)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,carrot)
        act LeftGrab(carrot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(carrot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftGrab(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftGrab(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Walk(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftGrab(carrot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Walk(carrot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightGrab(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftGrab(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Walk(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftGrab(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Walk(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightGrab(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Cut(carrot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightGrab(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightGrab(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightGrab(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act Close(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightGrab(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightGrab(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,mincedmeat)
        act LeftGrab(mincedmeat)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(mincedmeat)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightGrab(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,carrot)
        act LeftGrab(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftGrab(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightGrab(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftGrab(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        act Walk(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightGrab(cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftGrab(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        act Walk(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftGrab(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Walk(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,carrot)
        act LeftGrab(carrot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(carrot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,carrot)
        act LeftGrab(carrot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(carrot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftGrab(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,carrot)
        act LeftGrab(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftGrab(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Walk(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Walk(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftGrab(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftGrab(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Walk(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightGrab(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftGrab(bellpepper)
    sequence
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        act Walk(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightGrab(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftGrab(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        act Walk(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftGrab(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Walk(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Walk(stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Walk(cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Walk(stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Walk(stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightGrab(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act Walk(stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act RightGrab(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsHoldingKnife(self)
        cond IsOpen(cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsOpen(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Walk(stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightGrab(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightGrab(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act RightGrab(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightGrab(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,stove)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightGrab(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,stove)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,stove)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,stove)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,stove)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Cut(carrot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Cut(carrot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Cut(carrot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPut(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,stove)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsIn(mincedmeat,cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftGrab(carrot)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act Walk(carrot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,carrot)
        act LeftGrab(carrot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(carrot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,carrot)
        act LeftGrab(carrot)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(carrot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,carrot)
        act LeftGrab(carrot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(carrot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOpen(stove)
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftGrab(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Walk(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        cond IsRightHolding(self,mincedmeat)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftGrab(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        cond IsRightHolding(self,mincedmeat)
        act Walk(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftGrab(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftGrab(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Walk(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsLeftHolding(self,bellpepper)
        cond IsRightHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsStanding(self)
        act Walk(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftGrab(bellpepper)
    sequence
        cond IsRightHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        act Walk(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftGrab(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Walk(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftGrab(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Walk(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftGrab(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Walk(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftGrab(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Walk(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsIn(mincedmeat,cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act Walk(cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftGrab(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act Walk(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act Walk(cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftGrab(carrot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act Walk(carrot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsRightHolding(self,mincedmeat)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftGrab(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act Walk(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,mincedmeat)
        act LeftGrab(mincedmeat)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(mincedmeat)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(kitchenknife)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,carrot)
        act LeftGrab(carrot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(carrot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftGrab(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act Walk(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,mincedmeat)
        act LeftGrab(mincedmeat)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(mincedmeat)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Cut(carrot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act Walk(cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPutIn(mincedmeat,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(kitchenknife,cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(cookingpot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(carrot,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act RightPutIn(bellpepper,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(kitchenknife,cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPutIn(cookingpot,cookingpot)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOpen(stove)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsOpen(stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPutIn(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftGrab(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftGrab(cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftGrab(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftGrab(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsLeftHandEmpty(self)
        cond IsOpen(cookingpot)
        act LeftGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsOpen(cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        act Walk(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,cookingpot)
        cond IsClose(cookingpot)
        act Open(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        cond IsIn(mincedmeat,cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsIn(mincedmeat,cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsIn(mincedmeat,cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsIn(mincedmeat,cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        cond IsIn(mincedmeat,cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Cut(carrot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,stove)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsIn(bellpepper,cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,stove)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        cond IsClose(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsRightHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsRightHolding(self,kitchenknife)
        cond IsOpen(cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOpen(cookingpot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act LeftPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsLeftHolding(self,kitchenknife)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,stove)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act LeftPut(cookingpot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightPut(bellpepper,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsRightHolding(self,mincedmeat)
        act RightPut(mincedmeat,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,kitchenknife)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act RightPut(kitchenknife,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsRightHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act RightPut(carrot,stove)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,cookingpot)
        cond IsRightHolding(self,bellpepper)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsNear(self,stove)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act RightPut(bellpepper,stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Close(cookingpot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act Close(cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsHoldingKnife(self)
        act Cut(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(bellpepper,cookingpot)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Close(cookingpot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHolding(self,bellpepper)
        cond IsNear(self,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act LeftPutIn(bellpepper,cookingpot)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,carrot)
        act RightGrab(carrot)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act Walk(carrot)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsNear(self,mincedmeat)
        act RightGrab(mincedmeat)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,mincedmeat)
        act RightGrab(mincedmeat)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,mincedmeat)
        act RightGrab(mincedmeat)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsNear(self,mincedmeat)
        act RightGrab(mincedmeat)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsHoldingKnife(self)
        cond IsNear(self,mincedmeat)
        act RightGrab(mincedmeat)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,mincedmeat)
        act RightGrab(mincedmeat)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,mincedmeat)
        act RightGrab(mincedmeat)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsNear(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(bellpepper)
    sequence
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        cond IsNear(self,carrot)
        act RightGrab(carrot)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsIn(carrot,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightGrab(bellpepper)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(bellpepper)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsCut(carrot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsNear(self,stove)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsLeftHandEmpty(self)
        cond IsIn(carrot,cookingpot)
        cond IsCut(bellpepper)
        cond IsIn(mincedmeat,cookingpot)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsStanding(self)
        cond IsCut(carrot)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsNear(self,carrot)
        act RightGrab(carrot)
    sequence
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,mincedmeat)
        cond IsIn(carrot,cookingpot)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsOpen(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsNear(self,bellpepper)
        cond IsLeftHolding(self,carrot)
        cond IsOpen(cookingpot)
        cond IsCut(bellpepper)
        act RightGrab(bellpepper)
    sequence
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,carrot)
        cond IsLeftHolding(self,bellpepper)
        cond IsIn(bellpepper,cookingpot)
        cond IsOpen(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsHoldingKnife(self)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsNear(self,kitchenknife)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsClose(cookingpot)
        act RightGrab(kitchenknife)
    sequence
        cond IsOn(cookingpot,stove)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsClose(cookingpot)
        act Walk(kitchenknife)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,stove)
        cond IsClose(cookingpot)
        act RightPut(cookingpot,stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsClose(cookingpot)
        act Walk(stove)
    sequence
        cond IsRightHolding(self,cookingpot)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsClose(cookingpot)
        act Walk(cookingpot)
    sequence
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsStanding(self)
        cond IsClose(cookingpot)
        act Walk(cookingpot)
