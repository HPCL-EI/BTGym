selector
    sequence
        cond IsOpen(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
    sequence
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsClean(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsClean(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(rag)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHolding(self,book)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act Walk(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(rag)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(rag)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act LeftGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(rag)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act Walk(rag)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act Walk(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act Walk(rag)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHandEmpty(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act Walk(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act RightPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsClose(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsClose(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsRightHolding(self,juice)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(book)
        cond IsOpen(curtains)
        cond IsRightHolding(self,book)
        cond IsStanding(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOpen(curtains)
        cond IsStanding(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(book)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        act RightPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        cond IsOpen(curtains)
        cond IsStanding(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act LeftGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act RightPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act Walk(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        act Walk(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act RightGrab(rag)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act LeftGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOn(book,desk)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsLeftHandEmpty(self)
        act Walk(curtains)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsNear(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act RightGrab(rag)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        cond IsOn(juice,desk)
        act LeftPutIn(juice,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPutIn(rag,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsNear(self,desk)
        act LeftPutIn(book,desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsHoldingCleaningTool(self)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsOpen(desk)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,curtains)
        cond IsLeftHandEmpty(self)
        act Open(curtains)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act Open(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,juice)
        act LeftGrab(juice)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,book)
        act RightGrab(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act RightPut(rag,desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPutIn(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsOpen(desk)
        cond IsOpen(book)
        cond IsClean(desk)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsOpen(desk)
        cond IsClean(desk)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOpen(desk)
        cond IsRightHolding(self,juice)
        cond IsRightHolding(self,book)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(juice)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsRightHandEmpty(self)
        cond IsOpen(book)
        act Walk(book)
    sequence
        cond IsOpen(curtains)
        cond IsStanding(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        cond IsLeftHandEmpty(self)
        act Walk(book)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,juice)
        act RightGrab(juice)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsLeftHolding(self,juice)
        cond IsNear(self,desk)
        act LeftPut(juice,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act RightPut(rag,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsNear(self,desk)
        cond IsRightHolding(self,book)
        act LeftPut(book,desk)
    sequence
        cond IsNear(self,desk)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,book)
        cond IsLeftHandEmpty(self)
        act RightPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsOn(book,desk)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(rag,desk)
    sequence
        cond IsLeftHolding(self,book)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        act LeftPut(book,desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsHoldingCleaningTool(self)
        cond IsNear(self,desk)
        act Wipe(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsHoldingCleaningTool(self)
        cond IsRightHolding(self,rag)
        cond IsNear(self,desk)
        cond IsLeftHandEmpty(self)
        act Wipe(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsOpen(curtains)
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        cond IsOpen(book)
        cond IsRightHolding(self,rag)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsRightHolding(self,juice)
        cond IsLeftHandEmpty(self)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHandEmpty(self)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,rag)
        cond IsOn(juice,desk)
        act Walk(desk)
    sequence
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        cond IsLeftHolding(self,juice)
        act Walk(desk)
    sequence
        cond IsLeftHolding(self,rag)
        cond IsStanding(self)
        cond IsClose(book)
        cond IsClose(curtains)
        cond IsRightHolding(self,juice)
        act Walk(desk)
