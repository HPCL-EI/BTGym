selector
    sequence
        cond IsClean(desk)
        cond IsOn(folder,bookshelf)
        cond IsIn(paper,folder)
        cond IsClean(bookshelf)
    sequence
        cond IsClean(desk)
        cond IsRightHolding(self,paper)
        cond IsOpen(folder)
        cond IsOn(folder,bookshelf)
        cond IsNear(self,folder)
        cond IsClean(bookshelf)
        act RightPutIn(paper,folder)
    sequence
        cond IsClean(desk)
        cond IsRightHolding(self,paper)
        cond IsOpen(folder)
        cond IsOn(folder,bookshelf)
        cond IsStanding(self)
        cond IsClean(bookshelf)
        act Walk(folder)
    sequence
        cond IsClean(desk)
        cond IsRightHolding(self,paper)
        cond IsOpen(folder)
        cond IsOn(folder,bookshelf)
        cond IsStanding(self)
        cond IsNear(self,bookshelf)
        cond IsHoldingCleaningTool(self)
        act Wipe(bookshelf)
    sequence
        cond IsClean(desk)
        cond IsRightHolding(self,paper)
        cond IsOpen(folder)
        cond IsOn(folder,bookshelf)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        act Walk(bookshelf)
    sequence
        cond IsRightHolding(self,paper)
        cond IsOpen(folder)
        cond IsOn(folder,bookshelf)
        cond IsStanding(self)
        cond IsNear(self,desk)
        cond IsHoldingCleaningTool(self)
        act Wipe(desk)
    sequence
        cond IsRightHolding(self,paper)
        cond IsOpen(folder)
        cond IsOn(folder,bookshelf)
        cond IsStanding(self)
        cond IsHoldingCleaningTool(self)
        act Walk(desk)
    sequence
        cond IsRightHolding(self,paper)
        cond IsOpen(folder)
        cond IsOn(folder,bookshelf)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,rag)
        act LeftGrab(rag)
    sequence
        cond IsRightHolding(self,paper)
        cond IsOpen(folder)
        cond IsOn(folder,bookshelf)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        act Walk(rag)
    sequence
        cond IsOpen(folder)
        cond IsOn(folder,bookshelf)
        cond IsNear(self,paper)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        act RightGrab(paper)
    sequence
        cond IsOpen(folder)
        cond IsOn(folder,bookshelf)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        act Walk(paper)
    sequence
        cond IsOpen(folder)
        cond IsRightHolding(self,folder)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,bookshelf)
        act RightPut(folder,bookshelf)
    sequence
        cond IsOpen(folder)
        cond IsStanding(self)
        cond IsRightHolding(self,folder)
        cond IsLeftHandEmpty(self)
        act Walk(bookshelf)
    sequence
        cond IsOpen(folder)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsNear(self,folder)
        cond IsRightHandEmpty(self)
        act RightGrab(folder)
    sequence
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        cond IsClose(folder)
        cond IsNear(self,folder)
        cond IsRightHandEmpty(self)
        act Open(folder)
    sequence
        cond IsClose(folder)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        cond IsLeftHandEmpty(self)
        act Walk(folder)
